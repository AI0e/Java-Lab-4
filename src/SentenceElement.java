/**
 * A marker interface for any element that can be part of a Sentence,
 * such as a Word or a Punctuation mark.
 */
public interface SentenceElement {
    // This is a marker interface, so it has no methods.
    // It's used to group different types of objects in a single list.
}